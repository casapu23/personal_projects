<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="check_dates_cron" model="ir.cron">
        <field name="name">check the dates and update the working schedule of the employee</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_employee_working_schedule_period()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>

</odoo>
